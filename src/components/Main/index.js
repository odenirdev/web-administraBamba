import Styled from "styled-components";

const Index = Styled.main`
    width: 100%;
    max-width: 1200px;
    margin: 0 auto  auto;

    position: relative;
    z-index: 1;

    padding: 0.5rem;
`;

export default Index;
