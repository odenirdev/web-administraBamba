import styled from "styled-components";

export const Container = styled.div`
    table {
        cursor: pointer;
    }

    header {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
`;
